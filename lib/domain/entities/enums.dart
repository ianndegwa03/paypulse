enum CurrencyType {
  USD,
  EUR,
  GBP,
  JPY,
  ETH,
  BTC,
// ignore_for_file: constant_identifier_names
}

enum TransactionType {
  credit,
  debit,
  transfer,
  payment,
}

enum TransactionStatus {
  completed,
  pending,
  failed,
  cancelled,
}

enum UserRole {
  admin,
  employee,
  premium,
  standard,
}
