import 'package:flutter/material.dart';

class InsightsTabScreen extends StatelessWidget {
  const InsightsTabScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Insights Tab'),
      ),
    );
  }
}
