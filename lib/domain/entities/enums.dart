enum CurrencyType {
  USD,
  EUR,
  GBP,
  JPY,
  ETH,
  BTC,
}

enum TransactionType {
  credit,
  debit,
  transfer,
  payment,
}

enum TransactionStatus {
  completed,
  pending,
  failed,
  cancelled,
}

enum UserRole {
  admin,
  employee,
  premium,
  standard,
}
